% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{esem_compare}
\alias{esem_compare}
\title{Compare ESEM Against a Standard CFA}
\usage{
esem_compare(esem_model, cfa_model, data = NULL, estimator = NULL, ...)
}
\arguments{
\item{esem_model}{An \code{esem_fit} object from \code{\link{esem}}.}

\item{cfa_model}{A lavaan model string for the comparison CFA. All items
must be the same as in the ESEM.}

\item{data}{A \code{data.frame} used to fit the CFA. If \code{NULL}
(default), the data are re-extracted from the \code{esem_model} lavaan
object.}

\item{estimator}{Estimator for the CFA model. Defaults to the same estimator
used in \code{esem_model}.}

\item{...}{Additional arguments passed to \code{lavaan::cfa()}.}
}
\value{
A list of class \code{"esem_comparison"} with:
\describe{
  \item{\code{fit_table}}{A \code{data.frame} of fit indices for both models.}
  \item{\code{esem_fit}}{The original \code{esem_fit} object.}
  \item{\code{cfa_fit}}{The fitted \code{lavaan} CFA object.}
  \item{\code{lavtest}}{Output of \code{lavaan::lavTestLRT()} if models are nested, else \code{NULL}.}
}
}
\description{
Fits a user-specified CFA model on the same data and compares it against an
\code{esem_fit} object using fit indices and, where applicable, a chi-square
difference test (when models are nested).
}
\examples{
data("HolzingerSwineford1939", package = "lavaan")
d <- HolzingerSwineford1939[, paste0("x", 1:9)]

\donttest{
# Fit ESEM
esem_result <- esem(d, nfactors = 3)

# Define comparison CFA (no cross-loadings)
cfa_model <- "
  Visual  =~ x1 + x2 + x3
  Textual =~ x4 + x5 + x6
  Speed   =~ x7 + x8 + x9
"
comparison <- esem_compare(esem_result, cfa_model, data = d)
print(comparison)
}

}
\seealso{
\code{\link{esem}}
}
