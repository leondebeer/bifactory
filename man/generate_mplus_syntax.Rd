% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mplus_syntax.R
\name{generate_mplus_syntax}
\alias{generate_mplus_syntax}
\title{Generate Mplus Syntax for ESEM with Target Rotation}
\usage{
generate_mplus_syntax(
  factors,
  cfa_factors = NULL,
  regressions = NULL,
  covariances = NULL,
  data_file = "mydata.dat",
  missing_code = 999,
  estimator = "MLR",
  output_path = NULL
)
}
\arguments{
\item{factors}{Named list mapping factor names to their primary indicator
names. Example: \code{list(EX = c("y1","y2"), MD = c("y3","y4"))}.}

\item{cfa_factors}{Optional named list of additional CFA factors.}

\item{regressions}{Optional character vector of regression statements.}

\item{covariances}{Optional character vector of covariance statements.}

\item{data_file}{Character. Name of the data file. Default \code{"mydata.dat"}.}

\item{missing_code}{Numeric. Missing value code. Default \code{999}.}

\item{estimator}{Character. Default \code{"MLR"}.}

\item{output_path}{Character. Full path to write the .inp file. Returns
syntax string invisibly if \code{NULL}.}
}
\value{
The Mplus syntax as a character string (invisibly). Writes file
  if \code{output_path} is supplied.
}
\description{
Generates a complete Mplus .inp file for ESEM with target rotation,
using the correct Mplus syntax: separate BY statements per factor,
each with its own (*1) label. Primary items always appear first,
followed by cross-loading items targeted to zero.
}
\details{
Correct output format:
\preformatted{
EX BY
  batEX1 batEX2 ... batEX8       <- primary items first
  batMD1~0 ... batCI5~0 (*1);    <- cross-loadings after

MD BY
  batMD1 batMD2 ... batMD5       <- primary items first
  batEX1~0 ... batCI5~0 (*1);    <- cross-loadings after
}
}
\examples{
\dontrun{
syntax <- generate_mplus_syntax(
  factors = list(
    EX = c("batEX1","batEX2","batEX3","batEX4",
           "batEX5","batEX6","batEX7","batEX8"),
    MD = c("batMD1","batMD2","batMD3","batMD4","batMD5"),
    CI = c("batCI1","batCI2","batCI3","batCI4","batCI5")
  ),
  cfa_factors  = list(Burnout = c("mbiEX1z","mbiEX2z","mbiEX3z",
                                   "mbiEX4z","mbiEX5z")),
  regressions  = c("Burnout ON EX MD CI", "EX MD CI ON sex"),
  covariances  = c("mbiEX1z WITH mbiEX2z"),
  output_path  = file.path(tempdir(), "esem_model.inp")
)
cat(syntax)
}

}
